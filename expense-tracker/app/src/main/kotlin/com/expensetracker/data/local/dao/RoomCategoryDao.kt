@file:Suppress("ktlint:standard:no-empty-file")

package com.expensetracker.data.local.dao

// ANDROID-ONLY — This file requires the Android Gradle plugin + Room.
// Uncomment imports and interface body when migrating.
//
// import androidx.room.Dao
// import androidx.room.Query
// import androidx.room.Upsert
// import com.expensetracker.data.local.entity.CategoryEntity
//
// /**
//  * Room implementation of [CategoryDao].
//  *
//  * Extending [CategoryDao] is the compile-time guarantee that the Room SQL queries
//  * satisfy the plain-Kotlin interface used by seed and migration logic.
//  */
// @Dao
// interface RoomCategoryDao : CategoryDao {
//
//     @Query("SELECT * FROM categories")
//     override suspend fun findAll(): List<CategoryEntity>
//
//     @Query("SELECT * FROM categories WHERE id = :id LIMIT 1")
//     override suspend fun findById(id: String): CategoryEntity?
//
//     @Upsert
//     override suspend fun upsertAll(entities: List<CategoryEntity>)
// }
